[pytest]
testpaths = tests
markers =
    unit: deterministic pure-function and utility tests.
    mocked_graph: graph structure tests with mocked LLM responses.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
